# Copy to .env and fill in. PUBLIC_* vars are available in the browser (do not put secrets here).

# Required — Mapbox public access token (starts with pk.)
PUBLIC_MAPBOX_TOKEN=

# Optional — override JSON URL (default: /data/SummerGame2025.json from public/)
# PUBLIC_DATA_URL=https://example.com/locations.json
